name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  XCODE_DESTINATION: 'platform=iOS Simulator,name=iPhone 16 Pro,OS=latest'

jobs:
  test_meme:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Clear Derived Data for specific project
        run: |
          find ~/Library/Developer/Xcode/DerivedData -type d -name "Meme-*" -exec rm -rf {} +

      - name: Xcode Clean
        run: |
          xcodebuild clean -workspace Meme.xcworkspace -scheme Meme -destination "${{ env.XCODE_DESTINATION }}"

      - name: Install dependencies
        run: |
          xcodebuild -resolvePackageDependencies

      - name: Create plist file with sensitive data
        run: |
          echo '<?xml version="1.0" encoding="UTF-8"?>' > WebAPI/WebAPI/Base/APIConfig.plist
          echo '<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">' >> WebAPI/WebAPI/Base/APIConfig.plist
          echo '<plist version="1.0">' >> WebAPI/WebAPI/Base/APIConfig.plist
          echo '<dict>' >> WebAPI/WebAPI/Base/APIConfig.plist
          echo '  <key>API_KEY</key>' >> WebAPI/WebAPI/Base/APIConfig.plist
          echo "  <string>testAPIKey</string>" >> WebAPI/WebAPI/Base/APIConfig.plist
          echo '</dict>' >> WebAPI/WebAPI/Base/APIConfig.plist
          echo '</plist>' >> WebAPI/WebAPI/Base/APIConfig.plist

      - name: Run Meme tests
        run: |
          xcodebuild test -workspace Meme.xcworkspace -scheme Meme -destination "${{ env.XCODE_DESTINATION }}"

      - name: Run WebAPI tests
        run: |
          xcodebuild test -workspace Meme.xcworkspace -scheme WebAPI -destination "${{ env.XCODE_DESTINATION }}"
